!!!
%html
  %head
    %meta{:charset => "utf-8"}
    %meta{"http-equiv" => "X-UA-Compatible", :content => "IE=edge,chrome=1"}
    %title= content_for?(:title) ? yield(:title) : "Carogoverno"
    %meta{:content => "", :name => "description"}
    %meta{:content => "", :name => "author"}
    = stylesheet_link_tag "bootstrap.min", "stile"
    = javascript_include_tag "jquery-1.7.2.min", "bootstrap.min", "application", "rails"
    = csrf_meta_tags
    = yield(:head)
  %body
    %header.navbar.navbar-fixed-top
      %nav.navbar-inner
        .container
          = render 'layouts/navigation'
    %section.container#news
      .alert.alert-block
        %i.icon-bullhorn
        news qui??????
    #main{:role => "main"}
      .container
        .content
          .row
            .span12
              = render 'layouts/messages'
              = yield
    %footer.navbar-fixed-bottom.container
      %p.pull-right
        %a(href='#')
          Torna su
          %i.icon-chevron-up
